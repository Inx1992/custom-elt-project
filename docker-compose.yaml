version: '3'

services:
  source_postgres:
    image: postgres:15
    ports: 
      - "5433:5432"
    networks:
      - elt_network
    environment:
      POSTGRES_DB: source_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${SOURCE_POSTGRES_PASSWORD}
    volumes:
      - ./source_db_init/init.sql:/docker-entrypoint-initdb.d/init.sql

  destination_postgres:
    image: postgres:15
    ports: 
      - "5434:5432"
    networks:
      - elt_network
    environment:
      POSTGRES_DB: destination_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DEST_POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d destination_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  # elt_script:
  #   build:
  #     context: ./elt
  #     dockerfile: Dockerfile
  #   command: ["python", "elt_script.py"]
  #   networks:
  #     - elt_network
  #   depends_on:
  #     source_postgres:
  #       condition: service_started
  #     destination_postgres:
  #       condition: service_healthy

  # dbt:
  #   image: ghcr.io/dbt-labs/dbt-postgres:1.7.latest
  #   command: 
  #     [
  #       "run",
  #       "--profiles-dir",
  #       "/root",
  #       "--project-dir",
  #       "/dbt"
  #     ]
  #   networks:
  #     - elt_network
  #   volumes:
  #     - ./custom_postgres:/dbt
  #     - ~/.dbt:/root
  #   # ТУТ ПРАВИЛЬНІ УМОВИ:
  #   depends_on:
  #     destination_postgres:
  #       condition: service_healthy
  #     elt_script:
  #       condition: service_completed_successfully
  #   environment:
  #     DBT_PROFILE: custom_postgres
  #     DBT_TARGET: dev
  
  postgres:
    image: postgres:15
    networks:
      - elt_network
    environment: 
      - POSTGRES_USER=airflow
      - POSTGRES_PASSWORD=${AIRFLOW_DB_PASSWORD}
      - POSTGRES_DB=airflow
    # healthcheck:
    #   test: ["CMD-SHELL", "pg_isready -U airflow -d airflow"]
    #   interval: 5s
    #   timeout: 5s
    #   retries: 5
    #   start_period: 30s

  init-airflow:
    image: apache/airflow:2.10.2
    depends_on:
    - postgres
    networks:
      - elt_network
    environment:
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgres+psycopg2://airflow:${AIRFLOW_DB_PASSWORD}@postgres/airflow
    command: >
      bash -c "airflow db init &&
               airflow users create --username ${AIRFLOW_ADMIN_USER} --password ${AIRFLOW_ADMIN_PASSWORD} --firstname John --lastname Doe --role Admin --email adming@example.com"

  webserver:
    build: 
      context: .
      dockerfile: Dockerfile
    user: root
    depends_on:
      - postgres
    networks:
      - elt_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./airflow/dags:/opt/airflow/dags
      - ./elt:/opt/airflow/elt
      - ./custom_postgres:/opt/dbt
      - ~/.dbt:/root/.dbt
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - LOAD_EX=n
      - EXECUTOR=Local
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgres+psycopg2://airflow:${AIRFLOW_DB_PASSWORD}@postgres/airflow
      - AIRFLOW__CORE_FERNET_KEY=${AIRFLOW_FERNET_KEY}
      - AIRFLOW__WEBSERVER__DEFAULT__USER_USERNAME=${AIRFLOW_ADMIN_USER}
      - AIRFLOW__WEBSERVER__DEFAULT__USER_PASSWORD=${AIRFLOW_ADMIN_PASSWORD}
      - AIRFLOW_WWW_USER_USERNAME=${AIRFLOW_ADMIN_USER}
      - AIRFLOW_WWW_USER_PASSWORD=${AIRFLOW_ADMIN_PASSWORD}
      - AIRFLOW__WEBSERVER__SECRET_KEY=${AIRFLOW_SECRET_KEY}
    ports:
      - "8080:8080"
    command: webserver  

  scheduler:
    build: 
      context: .
      dockerfile: Dockerfile
    user: root
    depends_on:
      init-airflow:
        condition: service_completed_successfully
    networks:
      - elt_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./airflow/dags:/opt/airflow/dags
      - ./elt:/opt/airflow/elt
      - ./custom_postgres:/opt/dbt
      - ~/.dbt:/root/.dbt
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - LOAD_EX=n
      - EXECUTOR=Local
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgres+psycopg2://airflow:${AIRFLOW_DB_PASSWORD}@postgres/airflow
      - AIRFLOW__CORE_FERNET_KEY=${AIRFLOW_FERNET_KEY}
      - AIRFLOW__WEBSERVER__DEFAULT__USER_USERNAME=${AIRFLOW_ADMIN_USER}
      - AIRFLOW__WEBSERVER__DEFAULT__USER_PASSWORD=${AIRFLOW_ADMIN_PASSWORD}
      - AIRFLOW_WWW_USER_USERNAME=${AIRFLOW_ADMIN_USER}
      - AIRFLOW_WWW_USER_PASSWORD=${AIRFLOW_ADMIN_PASSWORD}
      - AIRFLOW__WEBSERVER__SECRET_KEY=${AIRFLOW_SECRET_KEY}
    command: scheduler

networks:
  elt_network:
    driver: bridge